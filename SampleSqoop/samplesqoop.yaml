apiVersion: v1
kind: Pod
metadata:
  name: sqoop
  labels:
    name: sqoop
spec:
  containers:
  - name: sqoop
    image: <Image>
    resources:
      limits:
        memory: "128Mi"
        cpu: "500m"
    ports:
      - containerPort: <Port>
